# Custom machine configuration for Raspberry Pi Compute Module 4
# with 4 GB RAM, onboard WiFi (BCM43455) and 32 GB eMMC.
#
# Inherits the upstream raspberrypi-cm4 definition and adds
# WiFi-specific firmware and kernel configuration fragments.

require conf/machine/raspberrypi-cm4.conf

# ── Human-readable description ───────────────────────────────────────────────
MACHINEOVERRIDES =. "raspberrypi-cm4-wifi:"

# ── WiFi firmware (Broadcom BCM43455 / CYW43455) ─────────────────────────────
# The firmware package is supplied by meta-raspberrypi.
MACHINE_EXTRA_RRECOMMENDS += " \
    linux-firmware-rpidistro-bcm43455 \
    linux-firmware-rpidistro-bcm43430 \
    brcm-patchram-plus \
"

# ── Connman / wpa_supplicant for WiFi management ─────────────────────────────
MACHINE_FEATURES += " wifi bluetooth"
